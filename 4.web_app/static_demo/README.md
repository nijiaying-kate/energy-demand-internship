# Static demo

This self-contained page uses only invented demonstration values. Open `index.html` locally or deploy this directory with a static hosting service. It does not require private data, a Python runtime, or a package installation.
