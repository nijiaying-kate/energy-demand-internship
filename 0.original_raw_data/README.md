# Synthetic input data

`location_demo_transactions.csv` is independently generated with fixed seed `20250301` by `generate_synthetic_transactions.py`.

It contains invented dates, identifiers, quantities, and demand patterns solely for reproducibility. It is not derived from private records, summaries, dates, identifiers, or business results.
